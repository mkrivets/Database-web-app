@model IEnumerable<Cats.CatTemper>

@{
    ViewData["Title"] = "Index";
}
<head>
    <style>
        body {
            background-image: url(https://images.unsplash.com/photo-1494256997604-768d1f608cac?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1101&q=80);
            background-repeat: no-repeat;
            background-attachment: fixed;
            background-size: cover;
        }

        table {
            background-color: rgba(0, 0, 0, 0.55);
            border-bottom-color: black;
        }

        .display-5 {
            color: white;
        }

        .row {
            background-color: rgba(0, 0, 0, 0.45);
            color: white;
        }

        .loc-r {
            margin-right: 4px;
        }

        .btn {
            background-color: black;
            border: 2px solid white;
        }
    </style>
</head>


<h1 class="display-5">Tempers & Cats</h1>

<p>
    <a class="display-5" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr class="display-5">
            <th>
                @Html.DisplayNameFor(model => model.Cat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Temper)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr class="display-5">
                <td>
                    @Html.DisplayFor(modelItem => item.Cat.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Temper.Name)
                </td>
                <td>
                    <a class="display-5" asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a class="display-5" asp-action="Details" asp-route-id="@item.Id">More details about cat</a> |
                    <a class="display-5" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
